## Date: 2026-05-05

### What I tried
- Instead of doing `| sort | uniq`, it's better to do `sort -u`
- Instead of `wc -l "$FILE" | awk '{print $1}'` to strip the filename, we can do `wc -l < "$FILE"`. Thsi is because `$1` could be empty leading to cryptic errorc

### What broke
-

### What I learned
-

### Commands/code worth remembering
```code

```

### Questions for later
-